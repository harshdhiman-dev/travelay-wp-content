/**
 * Set up an instance of the block
 *
 * @param {HTMLElement} element DOM Node of the block
 * @returns {void}
 */

// eslint-disable-next-line
function setupDsTabsBlock(element) {}

const allDsTabsBlocks = document.querySelectorAll('.wp-block-ds-theme-ds-tabs');
allDsTabsBlocks.forEach(setupDsTabsBlock);
